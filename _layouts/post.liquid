<!DOCTYPE html>
<html>
  <head>
    {% include "head.liquid" %}
  </head>
  <body class="{{ page.data.route }}">
    <header>
      {% include "menu.liquid" %}
      <h1>{{ page.title }}</h1>
    </header>
    <main>
      {{ page.content }}
    </main>
    <footer>
      {% include "footer.liquid" %}
    </footer>
  </body>
</html>
