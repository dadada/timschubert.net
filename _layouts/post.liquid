<!DOCTYPE html>
<html>
  <head>
    {% include "head.liquid" %}
  </head>
  <body class="{{ page.data.route }}">
    <header>
      {% include "menu.liquid" %}
    </header>
    <main>
      <h1>{{ page.title }}</h1>
      {{ page.content }}
    </main>
    <footer>
      {% include "footer.liquid" %}
    </footer>
  </body>
</html>
